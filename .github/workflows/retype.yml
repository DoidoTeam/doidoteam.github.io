name: Publish Retype powered website to GitHub Pages
on:
  workflow_dispatch:
  push:
    branches:
      - source

jobs:
  publish:
    permissions:
      contents: write
    name: Publish to main branch
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: retypeapp/action-build@latest
      - uses: DoidoTeam/action-github-pages@43e525e21d36478aca82838bbbcbd33505691376
        with:
          branch: main
          update-branch: true