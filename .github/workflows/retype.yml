name: Publish Retype powered website to GitHub Pages
on:
  workflow_dispatch:
  push:
    branches:
      - source

jobs:
  publish:
    permissions:
      contents: write
    name: Publish to main branch
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: retypeapp/action-build@latest
      - uses: DoidoTeam/action-github-pages@latest
        with:
          branch: main
          update-branch: true